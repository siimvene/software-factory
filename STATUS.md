# Status ledger

Which parts of the design sit in which evidence class, as of 2026-09-15. See
[WRITING.md](WRITING.md) for the classes.

| Mechanism | Class | Where | Note |
|---|---|---|---|
| The loop end to end on a real product | measured | kvart cycles 1, 2, 3, 5 | 12 to 19 min to PR; <30 min to live without required CI (cycles 1, 2), 41 min with the ruleset's five required checks on hosted runners (cycle 3, 2026-09-09). Cycle 5 (2026-09-10, epic KVART-19, two stories, three build stations): pickup 08:34Z, both PRs open 14:36Z, CI green 14:50Z and 14:52Z, merged 17:26Z and 17:38Z on the owner's word (the second PR needed a main merge and fresh receipts: the ruleset wants branches up to date and auto-merge is off), live 17:41Z, tickets Done by the write-back; 9 h 07 min pickup to live, of which 2 h 26 min was the merge wait; of the rest, ~5 h was contention on one Mac (stations 2 to 3x over cap, Sonar 40 min instead of 30 s, six environmental browser-gate failures) plus a machine restart; serialised on an idle machine the whole receipt tail for both PRs took 10 min. One machine runs one station and one reviewer at a time; scanners and the browser gate alone. Merges wait on the owner |
| Operating contract loaded in every session | measured | kvart, legacy core | eval suite on contract changes: proposed |
| Context standard L0 + team-context repo | measured | kvart | L1 ADRs bootstrapped; L2 docs-lint disabled on the plan |
| Spec retro-generation and findings file | measured | kvart | skills run by hand; sister spec PRs hand-edited |
| Day 1 onboarding runbook (team repo, standard adoption, memory move, specs, ADRs, PM workspace) | measured | kvart | one session for ~250 features; see 17-onboarding |
| ADR candidate table and rejected list | measured | kvart | 30 candidates, 13 + 8 written, 1 unconfirmed |
| PM workspace and skill catalogue | measured (workspace) / designed (most skills) | kvart | only the ticket and readiness skills exercised in the cycles; see 14-pm-surface. 2026-09-15, from the group's planning session: PMs hold engineer-level tooling and access (decided, authority unchanged); tracker fields deduced and approved against a `TRACKER.md` configured once (designed); attachments via REST because the hosted tracker MCP has no attachment call (measured: no such tool); prototype publishing is an explicit PM decision |
| Design system as a knowledge-plane layer | measured (first cycle) | kvart | repo built on Day 1; prototype-on-the-ticket decided 2026-09-09; cycle 5 (KVART-19, 2026-09-10) was the first UI cycle: the PM design handoff (portfolio status table: markers, states, thresholds) sat in the team-context repo and was a named input of the web build station; the station built to it without a design finding in either review leg; the pure marker module carries the thresholds in one constant. Not yet read by a QA pass; see 15-design-system |
| Concept-to-tool inventory | reference | | see 16-inventory |
| Memory store bound read-only; promote PR | measured | kvart | store moved; first promote PR opened by the bot identity in cycle 3 (2026-09-09), code owner reviews |
| Cross-vendor review, three axes | measured | kvart, legacy core | two backends; hard-fail proven |
| Blind security side-pass | measured | kvart | |
| Scanner tier | measured | kvart | static analysis scope excludes the gate code itself; in a worktree without the analysis properties file the tier silently degrades to dependency scanning only, the report must say SKIPPED (2026-09-09) |
| Static analysis as a receipt-checked pre-push gate (local scan scoped by changed sources, receipt bound to tree + project set, same hook as the browser receipt) | measured | kvart | KVART-16 (#43) merged and live 2026-09-09 19:31Z: backend analysis 24–46 s, frontend 24–33 s per run; verdict decided in the repo (coverage informational, cleat owns changed-line coverage); receipts per tree and project set, shared helpers with the browser gate; one cross-vendor panel (Codex 8 / Gemini 0) + security pass, 6 fixed; main moved twice under the PR and each merge cost ~3 min of receipts plus a strict-baseline stop, which is what decided ADR 0004 (b) the same night |
| Browser QA pass | measured | kvart | Until 2026-09-09 it ran only AFTER merge as the pre-deploy gate; KVART-15 (#42) moved it before the PR: scope derived from the specs' own routes and the API modules' own routers, a receipt per git tree, the pre-push hook refusing a browser-reaching diff without one; scoped 23 s, FULL 93 s; 29 review findings on the gate itself, 28 fixed; the CI e2e job is the follow-up. Runtime (adr/0008, KVART-18, 2026-09-10): the exploratory pass drives the Playwright CLI headless with per-persona sessions and saved state; the skill is tracked (it was gitignored, so no worktree had it). Shakedown plus one cold rehearsal by a fresh agent on the running stack: 2 surfaces, one mutating interaction verified through reload and the API, 0 product findings above MINOR, 4 defects in the skill text found and fixed. Gate on the skill itself: blind security pass 1 SERIOUS (wording) + 2 MINOR, panel Codex 9 / Gemini 1, 12 fixed, 2 acknowledged, including a measured one: the CLI's session registry is global across directories, so teardown closes by name. PR opened by the bot identity; merge waits on Siim. First UI cycle on this runtime still to measure |
| Quality ratchets (cleat) in the agent loop | measured | kvart, legacy core | guard held against its operator; 11 upstream defects deferred; Stop-hook re-send and formatter-pass false positives fixed in the operator's fork and vendored back (kvart #23); upstream `svetdev/cleat#6` carries the first batch, the second has no PR yet; strict mode vs the agent guard deadlocked every improving PR; decided 2026-09-09 evening (ADR 0004, option b): the guard permits a write that only lowers a number; of the day's seven baseline collisions five were the merge-base diff bug (#41) and two the real deadlock (#33/#36, #43); option (b) shipped the same night as cleat `--tighten` (fork #1 at 828c65ee, kvart #45 at 2daf6eb4): the rewrite is refused whenever it would accept anything new or worse, a target without recorded provenance, provenance drift or a scoped run; the guard regex is unchanged; the suites plant the lowering write that passes and the raising write that is refused; two gates whose ratchet runs the other way (inventory, public API surface) get no `--tighten`, found by the blind security pass |
| Mutation kill rate as a ratchet on the touched modules (killed over total, per-module baseline, receipt per tree) | measured (baseline) / proposed (gate) | kvart | campaign 2026-09-15 with mutmut: 59 database-free modules 30.3 % (5,394 of 17,783 mutants), 81 database-coupled modules through the testbench 39.9 % (11,709 of 29,341), largest billing module 11.2 %, platform billing 31.3 %, identity module 17 %; changed-line coverage had passed every PR; database-coupled half needed a template-cloned database per run, flag-gated; the gate (scope by diff, refuse a drop, targets on the money lane) is designed in 05 and not wired; principle 3 extended to the test suite itself |
| Program design note before the plan on sized tickets (sizing rule, template, one review leg on the note, module list as the spillover scope, note as a review pack) | proposed (note, sizing, review leg) / measured (precursor) | kvart cycle 4, trial | the cycle-4 API contract plus code map is the measured precursor: three stations built to it in parallel on disjoint worktrees and both review legs caught its one wrong field name; the trial spent 1 of 4 hands rounds on a shape choice the brief left open; cycle 2's planning document carried pseudocode on a retired flag, found in the diff stage; adr/0010 |
| Fail-on-base check (new or changed tests red on the merge base with the branch's tests applied, green on the branch, exemptions declared, failure kind recorded) | proposed (gate) / measured (hand step) | kvart | the `red-before` row has been done by hand since cycle 1 (fix reverted, test rerun); the trial's MINOR was a test that would have passed with a `list(...)`; script not built, receipt shape shared with the browser and static-analysis gates; adr/0010 |
| Architecture diff (enola) with layer declaration | measured (gate) / designed (catch) | kvart | the shipped Stop hook could not fail; replaced by a check on the three provable explainers that blocks once, 7 planted cases (kvart #24); not yet the cause of a caught regression |
| Orientation map (ripwire) | designed | kvart | worktree-index behaviour unverified |
| YAGNI ladder (chisle, replaced ponytail 2026-09-10) | designed | kvart | same 7-rung ladder plus prose compression and a PostToolUse hook that elides oversized tool output; marketplace source pinned to tag v3.0.0 (the format takes a ref, not a commit, so the pin is a tag; cached content verified at the tag's commit); hooks read in full, skill exfiltration scan 0 findings in 59 files; swap reviewed by both vendors (1 HIGH each, the same unpinned-source finding, fixed); kvart #47 green, merge waits on Siim; trial on a complex task pending |
| Head-and-hands delegation | measured | kvart trial | works when the head verifies; fails when it waits |
| Worker tier routing (mid tier builds, top tier debugs and verifies, escalation by re-brief) | measured | kvart | A/B 2026-09-08, six mid-tier build cycles by 2026-09-09 with no tier-attributed regression; alias leak found the same day (two of three config dirs lacked the override, about 11,000 calls on the wrong tier since 2026-08-13); the routing audit reads the served model |
| Worktree isolation and shared-checkout discipline | measured | kvart | incident-derived; 55 worktrees and 103 merged or superseded branches (72 local, 31 remote) piled up because no step owned the delete, now the orchestrator's after merge |
| Sandbox: egress, toolchain | measured | legacy core | |
| Sandbox: scoped bot identity | measured (kvart) / blocked (legacy core) | kvart, legacy core | kvart: a GitHub App with contents + pull-requests write and no admin; cannot merge past the ruleset, cannot edit workflows; authored a real PR that was merged and deployed in cycle 3 |
| Verification net: aggregate coverage ratchet, characterization + mutation, database self-provisioning | measured | legacy core | message broker container blocked |
| Money tiering per class with self-testing predicate | measured | legacy core | |
| Layering as lint over declared dependencies | measured | legacy core | 0 violations, no exemptions |
| Local-only phase enforced by pre-push hook | measured | legacy core, kvart | kvart's hook ignores the ref list and gates a pure branch delete; open |
| Provenance trailer | measured (trailer) / proposed (rejection check) | kvart | |
| Branch protection as the merge gate | measured | kvart (ruleset, 2026-09-08), legacy core (org plan) | kvart moved to a paid plan; ruleset = PR + five required checks, empty bypass list, refused a direct push from owner and bot. Incident 2026-09-09 18:34Z: an agent merged #44 under the owner's login on a handoff's instruction, indistinguishable from a human merge in the ledger; decided the same evening that the loop does not merge and a handoff never instructs one (ADR 0006) |
| Per-stage wall-clock and tokens | measured | kvart | principal session tokens not instrumented |
| CI as a required check on a real product | measured | kvart | first full run 40 min backend (serial suite); tiered: 19 min impact-selected backend on the PR (hosted 2-core), 6 min full suite on the on-demand 8-core runner after merge; the checks gated a real merge in cycle 3. 2026-09-09: PR-path backend moved to the same on-demand runner (kvart #31, #32): full suite 23m23s hosted -> 11m15s, runner woken by a pull_request_target run of main's code so the WIF ref pin holds; three reviewers converged on the residual (unreviewed PR code on a stateful VM), accepted, stateless per-wake VM queued as phase 2. Same day, #34: the first-ever impact-selected run (#33) failed on the whole-suite coverage floor (tiering had shipped without that branch executing), a deleted test module now forces FULL, and a late nightly displaced a merge's pending run in the shared concurrency group (per-run groups now) | 2026-09-09 evening (#41): a scripts/ci script selects its own unit test instead of forcing FULL (two 12-min runs had been for a Jira script), and cleat diffs against the MERGE BASE (five baseline collisions in one day were a two-dot diff against main's tip). Measured: FULL 10m30s / 9,480 tests / 8 workers, queue on the single VM up to 11m44s
| Escalation channel (agent asks, named human decides in-thread, decision written back) | measured (round trip) / designed (Jira write-back) | kvart | 49 s decision latency on the synthetic test; responder allowlist and timeout-as-state proven; cycle 4 (2026-09-09): one thread per cycle carrying product questions AND the agent's own dismissals/deferrals with default + deadline, after a measured lapse where they went to the ledger only |
| Spec as a gate input (spec pack to the panel, spec-drift class, REFINE-SPEC disposition, delta receipt before push) | proposed | kvart KVART-23, KVART-24 | cycle 4 (2026-09-09): the three panels ran 11:16 to 12:36Z, the D1 to D5 thread was posted 12:53Z, so no reviewer had the spec or the decisions; the scope question was raised from code and adjudicated by the principal alone; pre-build spec PR 8 unmerged at build, delta PR 10 open with five hand-written refinements; cycle 5's delta open at handoff. adr/0009 |
| Flightlist: the setup sequence as legs with a check and an evidence pointer each (docs/18) | proposed | the next test project | compiled 2026-09-10 from 12, 17, 05, 06, 08, 14; no second project has run it yet, which is the point of the next one |
| Ticket ledger follows the loop (pickup stamps the assignee; PR open, CI verdict, merge and deploy drive the transitions) | measured | kvart | KVART-10 / PR #39, 2026-09-09: found by the cycle-4 process assessment (every transition hand-set by the operator, no assignee, no PR link, board columns unmapped); the merge leg moved the ticket 13 s after the merge; the Done leg is a standalone deploy write-back both deploy paths end with, marker on the server; two cross-vendor passes, 22 findings, all fixed; the introducing PR cannot exercise itself (both jobs run main's script); same day #40: events never move epics, title shorthand, live red→In Progress→green→Ready for LIVE proven on the PR itself; first real deploy write-back closed 6 tickets. 2026-09-09 evening: every Done ticket still read Unresolved, because the REST-created workflow set a status and never the Resolution field (status is not resolution in Jira); fixed with update-field post-functions on the terminal and reopen transitions, 14 tickets backfilled `[measured 2026-09-09]` |
| Contract-first parallel stations (backend and web built concurrently against one written API contract, disjoint worktrees) | measured | kvart cycle 4 | three stations (Opus backend, Sonnet web, Sonnet UI) from one code map and one contract; the web station's e2e spec waits for the merged backend; both vendors caught the one field the contract's source got wrong |
| Fix station after the panel (top tier, verify-before-fix, dismissals escalated) | measured | kvart cycle 4 | 19 findings adjudicated: 12 fixed by a top-tier fixer, 2 dismissed by running the claim, 2 deferred, all four non-fixes posted to the escalation thread |
| A batch of stories through the loop (epic, parallel stations, ordered merges, two-step deploy) | measured | kvart cycle 4 | pickup → backend PR 2 h 08 min, → backend live 3 h 57 min, → whole batch live 4 h 57 min (three human merges + one CI round ~2 h); the strict ratchet and the related-set rule were the two friction sources; see case study |
| Cost per feature, per commit, per tree-hour | measured | kvart | list-price equivalents |
| Factory-on-itself share (loop-infrastructure merges over all merges) | measured | kvart | 8 of 13 on 2026-09-09; settle threshold below one in five for two weeks is proposed |
| Revert rate, defect escape, lead time baselines | measured (kvart, Jun to Sep 2026) | kvart | 0 reverts in 920 commits, 84.7 % direct-to-main, PR lead time median 6 min on 22 PRs, 46.5 % of fixes review-attributed; deploy tags missing, so escape stays a proxy |
| Reviewer F1 benchmark set | proposed | | |
| Recommendation acceptance rate | field | Pipedrive | |
| PM acceptance with acceptance identity | proposed, critic-reviewed | | manual trial first |
| Parity replay against an oracle | proposed | | kvart's billing parity with the incumbent is the template |
| Blast-radius ladder L0 to L6 | mixed | see 11-scaling | L0, L1, L3 measured; L4 designed; L2, L5, L6 proposed |
| DAG scheduling, best-of-N, width promotion criteria | proposed | | |
| Evals on the agent-config surface | proposed | | the named biggest gap |
| Backend rule pack for the legacy stack | proposed | | seeded by the first turns |
| Work-graph identity and routing layer on the ticket tracker (claim at pickup with a session-scoped lease, close with notes, ready/blocked dependency queries as a first-class primitive, one ticket comment with a hidden marker edited in place instead of appended) | proposed | kvart (cycle-4 gap) | independently converges with Beads (`bd`, Yegge's agent issue tracker; r/ClaudeCode, 2026-09-11, about 100 parallel sessions across 30-plus repos) on the fix direction the cycle-4 process-gaps assessment already named: assignee set at pickup, transitions driven by events rather than the principal. Its own top comment names the race software-factory already hit once at n=1 (adr 0006, kvart #44): claims keyed to an identity with no session-scoped lease or expiry let two actors converge on the same work. The 2026-09-09 shared-checkout rule, claim a broken shared gate before fixing it, is the same fix as a convention rather than a mechanical fencing token, and has not been forced to hold past a handful of parallel stations yet |
